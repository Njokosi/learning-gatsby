import React from 'react'
import Layout from '../components/layout'

const blog = () => {
    return (
        <Layout>
            <h4>Setup</h4>
            <p>If all you need is a little boost to your admin interface this approach is definitely worth considering.</p>
        </Layout>
    )
}

export default blog
