import React from 'react'
import Layout from '../../components/layout'

const latest = () => {
    return (
        <Layout>
            Latest products
        </Layout>
    )
}

export default latest
